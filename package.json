{
  "name": "shared-run",
  "version": "1.0.0",
  "author": "Richard Pierce-Jones",
  "description": "A nodejs CLI tool for running shell commands against all local npm package dependencies.",
  "keywords": [
      "task", "parallel", "serial", "sequential", "run", "npm-scripts", "npm", "tool", "commandline", "command", "cli"
  ],
  "repository": {
      "type": "git",
      "url": "https://github.com/richardpj/shared-run.git"
  },
  "bugs": {
      "url": "https://github.com/richardpj/shared-run/issues"
  },
  "bin": {
    "shared-run": "./bin/shared-run.js"
  },
  "scripts": {
    "test": "istanbul test node_modules/mocha/bin/_mocha -- -R spec"
  },
  "license": "MIT",
  "dependencies": {
    "chalk": "^1.1.3",
    "lodash": "^4.15.0",
    "minimist": "^1.2.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.0.2"
  }
}
